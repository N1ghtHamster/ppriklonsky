package ru.job4j.array;

public class MatrixCheck {
    public boolean mono(boolean[][] data){
        if data.length == data.
        boolean result = false;
        for () {
        }
        return result;
    }
}
